// Copy this file to config.js and fill in your own values.
// config.js is git-ignored so your real key/secret never get committed.
export const API_BASE = "https://reqres.in/api";
export const API_KEY = "REPLACE_WITH_PROJECT_KEY"; // reqres.in project API key
export const API_ENV = "prod"; // reqres docs use "prod"; try "production" if requests fail
export const ADMIN_SECRET = "change-me"; // code that unlocks the Admin tab
export const COLLECTIONS = {
  predictions: "predictions",
  results: "results",
  config: "config",
};
